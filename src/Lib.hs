module Lib
    ( someFunc
    ) where


-- data Day = Monday | Tuesday | Wednesday | Thursday | Friday | Saturday | Sunday 
--     deriving (Eq, Ord, Show, Read, Bounded, Enum)  

-- data Workout = Workout {wtype :: String, date :: String} 
--     deriving (Show, Eq, Read)

-- getType :: Workout -> String
-- getType w = "???"

someFunc :: IO ()
someFunc = do
    -- let day = Monday
    -- let w = Workout {wtype="PECS", date="1111"}
    -- putStr (show wtype)
    putStrLn "A"
